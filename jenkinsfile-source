pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                git branch: 'main'
                echo "Build App"
                
            }
        }
    }
}
